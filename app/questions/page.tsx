import { PlaceholderPage } from "@/components/placeholder-page";

export default function QuestionsPage() {
  return (
    <PlaceholderPage
      title="Customer Questions"
      description="Give customers a structured way to send vehicle concerns or service questions and let the shop track responses."
      planned={["Customer concern inbox", "Vehicle and appointment context", "Shop responses", "Open, answered, and closed status"]}
    />
  );
}
